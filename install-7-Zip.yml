---
- name: Instalar 7-Zip en Windows y crear icono en escritorio
  hosts: windows
  gather_facts: false

  vars:
    sevenzip_url: "https://www.7-zip.org/a/7z2601-x64.exe"
    sevenzip_installer: "C:\\Windows\\Temp\\7zip-installer.exe"
    sevenzip_exe: "C:\\Program Files\\7-Zip\\7zFM.exe"

  tasks:
    - name: Validar si 7-Zip ya esta instalado
      ansible.windows.win_stat:
        path: "{{ sevenzip_exe }}"
      register: sevenzip_status

    - name: Descargar instalador oficial de 7-Zip
      ansible.windows.win_get_url:
        url: "{{ sevenzip_url }}"
        dest: "{{ sevenzip_installer }}"
      when: not sevenzip_status.stat.exists

    - name: Instalar 7-Zip en modo silencioso
      ansible.windows.win_package:
        path: "{{ sevenzip_installer }}"
        arguments: "/S"
        creates_path: "{{ sevenzip_exe }}"
        state: present
      when: not sevenzip_status.stat.exists

    - name: Crear icono de 7-Zip en el escritorio publico
      ansible.windows.win_powershell:
        script: |
          $exePath = "C:\Program Files\7-Zip\7zFM.exe"

          if (-not (Test-Path $exePath)) {
            throw "No se encontro el ejecutable de 7-Zip en $exePath"
          }

          $desktopPath = [Environment]::GetFolderPath('CommonDesktopDirectory')
          $shortcutPath = Join-Path $desktopPath '7-Zip File Manager.lnk'

          $shell = New-Object -ComObject WScript.Shell
          $shortcut = $shell.CreateShortcut($shortcutPath)
          $shortcut.TargetPath = $exePath
          $shortcut.WorkingDirectory = Split-Path $exePath
          $shortcut.IconLocation = "$exePath,0"
          $shortcut.Description = "7-Zip File Manager"
          $shortcut.Save()

          [pscustomobject]@{
            shortcut = $shortcutPath
            target = $exePath
          } | ConvertTo-Json

    - name: Validar instalacion desde el registro de Windows
      ansible.windows.win_shell: |
        $registryPaths = @(
          'HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*',
          'HKLM:\Software\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\*'
        )

        Get-ItemProperty -Path $registryPaths -ErrorAction SilentlyContinue |
          Where-Object { $_.DisplayName -like "*7-Zip*" } |
          Select-Object DisplayName, DisplayVersion, Publisher |
          ConvertTo-Json -Depth 3
      register: installed_app
      changed_when: false

    - name: Mostrar resultado
      ansible.builtin.debug:
        msg:
          aplicacion: "7-Zip"
          estado: "Instalado o ya existente"
          icono: "Creado en el escritorio publico"
          detalle: "{{ installed_app.stdout }}"
